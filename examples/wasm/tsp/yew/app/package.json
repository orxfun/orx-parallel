{
    "name": "orx-parallel-wasm-tsp-yew-app",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "scripts": {
        "build:wasm": "ORX_PARALLEL_WASM_BINDINGS=../components ORX_PARALLEL_WASM_OUT_DIR=./pkg node ../../../../../wasm-web/src/build.js build",
        "dev:full": "npm exec -- vite",
        "dev": "npm exec -- vite",
        "typecheck": "tsc --noEmit",
        "build": "npm run typecheck && npm exec -- vite build",
        "preview": "npm run build && npm exec -- vite preview"
    },
    "dependencies": {
        "highlight.js": "^11.11.1",
        "orx-parallel-web": "file:../../../../../wasm-web"
    },
    "devDependencies": {
        "typescript": "^5.6.3",
        "vite": "^5.4.10"
    }
}