{
    "name": "par-wasm-app-rspack",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "scripts": {
        "build:wasm": "ORX_PARALLEL_WASM_BINDINGS=../wasm_bindings ORX_PARALLEL_WASM_OUT_DIR=./pkg node ./node_modules/orx-parallel-wasm/dist/build.js build",
        "dev": "npm exec -- rspack serve --mode development",
        "typecheck": "tsc --noEmit",
        "build": "npm run build:wasm && npm run typecheck && npm exec -- rspack build --mode production"
    },
    "dependencies": {
        "orx-parallel-wasm": "git+https://github.com/orxfun/orx-parallel-wasm.git"
    },
    "devDependencies": {
        "@rspack/cli": "^1.0.14",
        "@rspack/core": "^1.0.14",
        "css-loader": "^7.1.2",
        "style-loader": "^4.0.0",
        "typescript": "^5.6.3"
    }
}