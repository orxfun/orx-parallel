{
    "name": "orx-parallel-wasm-perf-web",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "scripts": {
        "build:wasm:rayon": "RUSTUP_TOOLCHAIN=nightly RUSTFLAGS='-C target-feature=+atomics,+bulk-memory -C link-arg=--shared-memory -C link-arg=--max-memory=1073741824 -C link-arg=--import-memory -C link-arg=--export=__wasm_init_tls -C link-arg=--export=__tls_size -C link-arg=--export=__tls_align -C link-arg=--export=__tls_base' wasm-pack build ../crate_rayon --target web --out-dir ../web/pkg_rayon -- -Z build-std=panic_abort,std",
        "build:wasm:orx-rayon": "RUSTUP_TOOLCHAIN=nightly RUSTFLAGS='-C target-feature=+atomics,+bulk-memory -C link-arg=--shared-memory -C link-arg=--max-memory=1073741824 -C link-arg=--import-memory -C link-arg=--export=__wasm_init_tls -C link-arg=--export=__tls_size -C link-arg=--export=__tls_align -C link-arg=--export=__tls_base' wasm-pack build ../crate_rayon_orx --target web --out-dir ../web/pkg_rayon_orx -- -Z build-std=panic_abort,std",
        "build:wasm:orx": "RUSTUP_TOOLCHAIN=nightly RUSTFLAGS='-C target-feature=+atomics,+bulk-memory -C link-arg=--shared-memory -C link-arg=--max-memory=1073741824 -C link-arg=--import-memory -C link-arg=--export=__wasm_init_tls -C link-arg=--export=__tls_size -C link-arg=--export=__tls_align -C link-arg=--export=__tls_base' wasm-pack build ../crate_orx --target web --out-dir ../web/pkg_orx -- -Z build-std=panic_abort,std",
        "build:wasm": "npm run build:wasm:rayon && npm run build:wasm:orx-rayon && npm run build:wasm:orx",
        "dev:full": "npm run build:wasm && npm exec -- vite",
        "dev": "npm exec -- vite",
        "build": "npm exec -- vite build",
        "preview": "npm run build && npm exec -- vite preview"
    },
    "devDependencies": {
        "typescript": "^5.6.3",
        "vite": "^5.4.10"
    }
}