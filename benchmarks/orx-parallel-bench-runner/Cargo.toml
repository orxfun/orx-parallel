[package]
name = "orx-parallel-bench-runner"
version = "0.1.0"
edition = "2024"
publish = ["artifactory"]

[dependencies]
clap = { version = "4.6.6", features = ["derive"] }
serde = { version = "1.0", registry = "artifactory" }
serde_json = "1.0"
toml_edit = "0.22"
