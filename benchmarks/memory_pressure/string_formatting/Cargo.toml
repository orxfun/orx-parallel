[package]
name = "string_formatting"
version = "0.1.0"
edition = "2024"
publish = ["artifactory"]

[dependencies]
clap = { version = "4.6.6" }
orx-criterion = { version = "1.0.0" }
orx-parallel = { path = "../../../" }
orx-parallel-bench-helper = { path = "../../orx-parallel-bench-helper" }
rayon = { version = "1.12.0" }

[features]
default = []
seq = []
orx-once = []
orx-basic = ["orx-parallel/persistent-pool"]
orx-rayon = ["orx-parallel/persistent-pool-rayon"]
rayon = []
