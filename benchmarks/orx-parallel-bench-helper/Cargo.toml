[package]
name = "orx-parallel-bench-helper"
version = "0.1.0"
edition = "2024"
publish = ["artifactory"]

[dependencies]
clap = { version = "4.6.6", features = ["derive"] }
orx-criterion = { version = "1.0.0" }
